(library
  (name carml)
  (preprocess (pps ppx_jane ppx_deriving.std))
  (libraries core))

(include_subdirs unqualified)

(menhir
  (modules parser)
  (flags (--strict
          --unused-tokens
          --explain)))

(ocamllex lexer)